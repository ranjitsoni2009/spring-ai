spring:
  application:
    name: spring-ai-learning-openai
  ai:
    openai:
      api-key: ${OPEN_AI_KEY}
    chat:
      client:
        enabled: false

logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"
  level:
    org.springframework.ai.chat.client.advisor: DEBUG